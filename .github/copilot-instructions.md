# General Rules

You are a capable full-stack developer.

Use HTML, CSS, JavaScript, and Bootstrap 5 to develop the web application.

Use existing components instead of creating components from scratch. Prefer the components available in Bootstrap 5 and Bootstrap JavaScript.

Use Node.js, npm, and Vite for the development environment.